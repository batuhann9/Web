@* @model IEnumerable<BerberSalonu.Models.Randevu>

@{
    ViewData["Title"] = "Onay Bekleyen Randevular";
}

<h2>Onay Bekleyen Randevular</h2>

@if (TempData["Mesaj"] != null)
{
    <div class="alert alert-success">
        @TempData["Mesaj"]
    </div>
}

@if (!Model.Any())
{
    <p>Şu anda onay bekleyen bir randevu bulunmamaktadır.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Tarih</th>
                <th>Saat</th>
                <th>Müşteri</th>
                <th>Hizmet</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var randevu in Model)
            {
                <tr>
                    <td>@randevu.RandevuTarihi.ToShortDateString()</td>
                    <td>@randevu.RandevuTarihi.ToShortTimeString()</td>
                    <td>@randevu.Kullanici?.AdSoyad</td>
                    <td>@randevu.Yetenek?.Adi</td>
                    <td>
                        <form method="post" asp-action="Onayla" asp-controller="Berber" asp-route-id="@randevu.Id" style="display:inline;">
                            <button class="btn btn-success btn-sm">Onayla</button>
                        </form>
                        <form method="post" asp-action="Red" asp-controller="Berber" asp-route-id="@randevu.Id" style="display:inline;">
                            <button class="btn btn-danger btn-sm">Reddet</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
 *@